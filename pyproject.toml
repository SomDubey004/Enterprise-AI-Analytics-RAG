[project]
name = "enterprise-ai-analytics-rag"
version = "0.1.0"
description = "Enterprise AI Analytics and RAG Data Platform"
requires-python = ">=3.12,<3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
